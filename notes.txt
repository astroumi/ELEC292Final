

General:
--- app will preprocess data, split into 5s intervals, extract features, normalize features,
    give each 5s split to model, model will label/make column for prediction,
    app will nicely display output


TO DO:
- handle all types of edge cases across all files and functions



DONE:
- need to figure out how to normalize data for use with model prediction
- test not preprocessing data on algo
- extraction and training into functions, terminal interface to decide run type on main, also retrain classifier on new data?
- save trained classifier on disk, use within app
- need to figure out how to export model as variable